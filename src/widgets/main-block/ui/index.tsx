import {FC, ReactNode} from 'react';

interface MainBlock {
    title:ReactNode,
    description:ReactNode,
    tiles:[]
}

const MainBlock:FC = () => {
    return (
        <div>
            
        </div>
    );
};

export default MainBlock;